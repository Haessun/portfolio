import SectionTitle from "../SectionTitle";
import CertificateItem from "./AdditionalSkills";

const Certificate = ({}: {}) => {
    return (
        <div>
            <SectionTitle>Certificates</SectionTitle>
            <div className="flex flex-col gap-24">
            </div>
        </div>
    );
};

export default Certificate;
